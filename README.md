git clone -b master --single-branch https://github.com/happen-w/dotfile-lazy.git $env:LOCALAPPDATA\nvim

git clone -b master --single-branch https://github.com/happen-w/dotfile-lazy.git ~/.config/nvim
